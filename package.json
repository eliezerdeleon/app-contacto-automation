{
  "name": "app-contacto-automation",
  "version": "1.0.0",
  "description": "Repositorio para la automatizacion de APP CONTACTO, hecho con cypress.",
  "homepage": "https://github.com/eliezerdeleon/app-contacto-automation#readme",
  "bugs": {
    "url": "https://github.com/eliezerdeleon/app-contacto-automation/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eliezerdeleon/app-contacto-automation.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open:qa": "cypress open --env ENV=qa",
    "cy:open:dev": "cypress open --env ENV=dev",
    "cy:open:staging": "cypress open --env ENV=staging",
    "cy:open:prod": "cypress open --env ENV=prod",
    "cy:run:qa": "cypress run --env ENV=qa",
    "cy:run:dev": "cypress run --env ENV=dev",
    "cy:run:staging": "cypress run --env ENV=staging",
    "cy:run:prod": "cypress run --env ENV=prod"
  },
  "devDependencies": {
    "cypress": "^15.8.1",
    "cypress-real-events": "^1.15.0"
  }
}
